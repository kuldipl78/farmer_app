# Railway Environment Configuration
# This file shows the environment variables needed for Railway deployment

# Database Configuration (Railway will provide this automatically)
# DATABASE_URL=postgresql://username:password@host:port/database

# Application Configuration
PORT=8001
DEBUG=False
HOST=0.0.0.0

# CORS Configuration (update with your frontend URLs)
ALLOWED_ORIGINS=*

# Security (Railway will provide secure values)
# SECRET_KEY=your-secret-key-here
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30